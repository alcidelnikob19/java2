H1 Домашнее задание
Поместите исходные тексты с выполненным заданием в package ru.progwards.java1.lessons.helloworld

Задача 1. Класс Task1
1.1 Реализовать функцию public static void main(String[] args) в которой следует: создать три строковые переменные со значениями:
"Хорошо идут дела"
"Изучаю Java я!"
" "
вывести на консоль, используя только эти переменные и функции System.out.print(), System.out.println() следующий текст:
Хорошо идут дела
Изучаю Java я!
Хорошо идут дела Изучаю Java я!
Изучаю Java я! Хорошо идут дела

Задача 2. Класс Task2
2.1 Реализовать функцию public static void subtraction(int x, int y) в которой:
завести переменную int z и присвоить ей значение разности x - y;
вывести на консоль, используя переданные параметры, переменную z и функцию System.out.print(), System.out.println() следующий текст:
Вызвана функция subtraction() с параметрами x = ?, y = ?
Результат z = ?
где вместо знаков вопроса будут реальные значения.

2.2 Реализовать функцию public static void main(String[] args) из которой:
вызвать функцию subtraction() с параметрами x=89, y=55

Задача 3. Класс Task3
3.1 Реализовать функцию public static int addition(int x, int y) которая будет:
возвращать сумму x + y;
выводить на консоль, используя функцию System.out.println() следующий текст:
Вызвана функция addition()

3.2 Реализовать функцию public static int subtraction(int x, int y) которая будет:
возвращать разность x - y;
выводить на консоль, используя функцию System.out.println() следующий текст:
Вызвана функция subtraction()

3.3 Реализовать функцию public static int multiplication(int x, int y) которая будет:
возвращать произведение x * y;
выводить на консоль, используя функцию System.out.println() следующий текст:
Вызвана функция multiplication()

3.4 Реализовать функцию public static void main(String[] args) в которой следует:
объявить переменные типа int с названиями a, b, c;
переменной a присвоить значение 34, переменной b присвоить значение 55;
вывести значения переменных a и b на консоль так:
a = ?
b = ?
где вместо знаков вопроса будут их реальные значения;

после этого присвоить переменной c значение возвращённое функцией addition(), вызванной с параметрами x=a, y=b;
и вывести значения переменной c на консоль так:
a + b = ?
где вместо знака вопроса будет значение переменной c;

после этого присвоить переменной c значение возвращённое функцией subtraction(), вызванной с параметрами x=a, y=b;
и вывести значения переменной c на консоль так:
a - b = ?
где вместо знака вопроса будет значение переменной c;

после этого присвоить переменной c значение возвращённое функцией multiplication(), вызванной с параметрами x=a, y=b;
и вывести значения переменной c на консоль так:
a * b = ?
где вместо знака вопроса будет значение переменной c.